{
  "name": "tx-shared-interfaces",
  "version": "0.0.0",
  "description": "Interfaces",
  "main": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "format": "prettier --write \"src/**/*.ts\""
  }
}
